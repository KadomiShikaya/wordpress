version: '3.1'

services:

  https-portal:
    image: steveltn/https-portal:1.3.0
    ports:
      - '80:80'
      - '443:443'
    links:
      - wordpress
    environment:
      DOMAINS: 'ufq9r43-3409fjd5.tk -> http://wordpress'
    volumes:
      - https-portal-data:/var/lib/https-portal
    extra_hosts:
      - 'ufq9r43-3409fjd5.tk:13.230.146.160'

  wordpress:
    image: wordpress
    restart: always
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: exampleuser
      WORDPRESS_DB_PASSWORD: examplepass
      WORDPRESS_DB_NAME: exampledb
    volumes:
      - wordpress:/var/www/html

  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: exampledb
      MYSQL_USER: exampleuser
      MYSQL_PASSWORD: examplepass
      MYSQL_RANDOM_ROOT_PASSWORD: '1'
    volumes:
      - db:/var/lib/mysql
      
volumes:
  wordpress:
  db:
  https-portal-data:
  
